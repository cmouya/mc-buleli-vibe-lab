# M6.2 — Tenant-safe accepted Path persistence through owned Goal (implementation plan)

**Provenance:** Written as a plan after ADR-017 acceptance. **Do not execute from this file.** **M6.2 implementation has NOT started.**

**Depends on:** [`docs/m6.2-decisions.md`](m6.2-decisions.md), [ADR-017](architecture-baseline.md#adr-017--tenant-safe-accepted-path-persistence-through-owned-goal).

## Implementation status

**Plan frozen. Code not started.** Next executable checkpoint: **C1**.

## Sequence

1. ADR-017 **Accepted** (this documentation slice).
2. Future coding: C1 → C2 → C3 → FINAL VALIDATION. Stop after each checkpoint for human review. No commit unless separately approved.

## In scope

- Tenant-safe persist of an **accepted Path + nested Steps** under an **already-owned Goal**
- Owned Path application port / use case (not global `GoalRepository.getById` authorization)
- Drizzle persistence through existing `learning_paths.goal_id`
- `POST /api/v1/organizations/:organizationId/goals/:goalId/path`
- Unit, API, DB, boundary, security, build, Playwright GR **regression**

## Out of scope

Evidence persist HTTP; Path list/update/delete; Goal list/update/delete; protect confirm/generate; GR login or dual-write; Learner HTTP / auto-provision; Program; Cohort; Mastery; invitations; RBAC; SSO; session org/learner columns; Path/Step tenant FKs; fake backfill; UNIQUE(`goal_id`) without a cardinality ADR; migration 0008 unless a later human decision requires a new invariant.

## C1 — Owned Path application / port

**Goal:** Tenant-safe Path write abstraction without HTTP.

- Owned Path repository port (or equivalent) distinct from unscoped `LearningPathRepository`
- `persistOwnedPath` / `acceptOwnedPath` application use case
- Requires trusted `LearnerContext` / `GoalOwnerScope`
- Server-resolved **owned Goal** before persist
- `Path.goalId` stamped from that Goal (not client ownership fields)
- Nested Steps persisted with the Path
- Unit + architecture boundary tests (no Fastify/Drizzle in application)

**Stop:** before Drizzle. No HTTP. No migration. If port shape requires a new architecture decision, stop and report.

## C2 — Drizzle tenant-safe Path persistence

**Goal:** Persist through existing Goal ownership.

- Prove Goal belongs to `organizationId` + `learnerId` (scoped query, not global `getById`)
- Persist Path + nested Steps under that Goal
- No Path/Step tenant FKs
- Legacy/unowned Goal fails closed
- DB tests: owner success; cross-org; wrong Learner; legacy NULL/NULL
- **No 0008 expected**

**Stop:** if schema or cardinality (UNIQUE goal_id) becomes necessary — report for human decision; do not generate 0008.

## C3 — Owned Path HTTP

**Goal:** Expose `POST /api/v1/organizations/:organizationId/goals/:goalId/path`

Chain: cookie → `resolveSession` → OrganizationContext → LearnerContext → owned Goal → owned Path use case → scoped repository.

Security matrix:

- owner success
- unauthenticated → 401
- no membership → generic 403
- membership but no Learner → generic 403
- cross-org → generic 404
- same-org wrong Learner → generic 404
- unknown Goal → generic 404
- legacy NULL/NULL Goal → generic 404
- forged learnerId ignored
- forged organizationId outside path ignored
- forged body `goalId` cannot redirect ownership
- same User, two orgs isolated
- unknown vs inaccessible identical body

Public `POST /api/v1/paths/generate` remains 200 without cookie. Existing C7 GET routes remain.

## FINAL VALIDATION

- `npm run typecheck`
- `npm test`
- `npm run test:api`
- `npm run test:db`
- `npm run build`
- `npm run test:e2e`
- `git diff --check`

Audit: architecture boundaries; tenant isolation; migration history (still no unexpected 0008); Golden Reference regression; public confirm/generate; M6.1 regression; no M6.3 features.

Do not commit automatically.

## Authorization chain

`learnova.sid` → `resolveSession` → AuthContext → requested organizationId → `resolveOrganizationContext` → OrganizationContext → `resolveLearnerContext` → LearnerContext → owned Goal lookup → owned Path persist → repository constrained by tenant/learner and `goal_id`.
